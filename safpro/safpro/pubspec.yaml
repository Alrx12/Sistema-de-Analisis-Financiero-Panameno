name: safpro
dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  dio: ^5.3.2         # para subidas de archivos con progreso
  flutter_secure_storage: ^8.0.0
  provider: ^6.0.5    # o riverpod para gestión de estado
  file_picker: ^6.1.1 # seleccionar CSV/XLSX/PDF
  charts_flutter: ^0.12.0  # gráficos de barras y pasteles