/**
 * Este archivo existe solo para que expo-router reconozca la carpeta analysis/
 * La lista de análisis vive en (tabs)/analysis.tsx
 * El detalle está en (tabs)/analysis/[id].tsx
 * Sin export default — expo-router ignora este archivo como ruta.
 */
