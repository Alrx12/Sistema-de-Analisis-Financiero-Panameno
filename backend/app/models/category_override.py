# DEPRECATED — este modelo no se usa en producción.
# Las correcciones de categoría se manejan vía FinancialClassifier.learn()
# que persiste en los archivos JSON de Knowledge Base, no en la DB.
#
# TODO: eliminar este archivo del repositorio.
# git rm backend/app/models/category_override.py
