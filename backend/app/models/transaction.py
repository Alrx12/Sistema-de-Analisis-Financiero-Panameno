# DEPRECATED — este modelo no se usa en producción.
# Las transacciones crudas nunca se persisten en DB (decisión de diseño).
# Solo AnalysisSnapshot (JSON agregado) se guarda.
#
# TODO: eliminar este archivo del repositorio.
# git rm backend/app/models/transaction.py
